node_modules/
*.log
lotchance-netlify.zip

# Android build artefacts (the Capacitor-synced assets under
# android-app/android/app/src/main/assets/public ARE tracked).
android-app/android/.gradle/
android-app/android/build/
android-app/android/app/build/
android-app/android/local.properties
android-app/node_modules/

# Generated data IS committed on purpose — it is what the app downloads.
# Only the local scratch output is ignored.
